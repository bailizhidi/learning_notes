# MeshGraphNets论文解读

[查看论文 PDF](images/MeshGraphNets论文解读/MeshGraphNets论文.pdf)

## 1.提出此方法的原因及其特点
网格表示能够支持强大的数值积分方法，并且网格的分辨率可以自适应调整，然后在精度和效率之间实现更有利的权衡。

• 高维科学仿真的计算代价高。

• 求解器与参数往往需要针对所研究的每个系统进行调试与标定。

MeshGraphNets：是一种使用图神经网络学习网格仿真的框架，模型能够在网格图上进行消息传递，并在前向仿真过程中自适应地调整网格离散化。可以预测多种物理系统的动力学行为：空气动力学；结构力学；布料。模型自适应能力支持学习与分辨率无关的动力学规律，并在测试阶段可以扩展到更复杂的状态空间，运行速度比原始仿真快1-2个数量级。

## 2.方法具体介绍
自适应网格划分使得仿真能够达到用规则离散方案难以实现的精度与分辨率水平。该文章提出了一种用于预测物理系统动力学的方法，充分利用了自适应网格的优势，方法通过将仿真状态编码成一张图，在两个相互独立的空间中进行计算：一是仿真网格张成的网格空间，二是仿真流形嵌入的欧氏世界空间。

```{figure} images/MeshGraphNets论文解读/模型架构.png
:width: 700px
:align: center

该模型采用 Encode–Process–Decode（编码–处理–解码） 架构，并使用单步监督（one-step supervision）进行训练；在推理阶段可以通过迭代应用（iterative rollout）生成长时间轨迹。编码器将输入网格Mt转换为图结构，并添加额外的世界空间边（world-space edges）。处理器在网格边（mesh edges）与世界边（world edges）上进行多轮消息传递，更新所有节点与边的嵌入表示。解码器为每个节点输出加速度，并据此更新网格，得到 Mt+1。
```

结论：该方法可以直接从数据中学习差异巨大的物理系统的动力学行为。证明出通过在网格空间上进行计算，可以可靠的建模具有静止/参考状态的材料（弹性体），这类材料对于无网格的预测模型具有挑战性。当真实场景中的物理模型、参数或边界条件并不完全已知时，学习式仿真对于现实世界预测也很有价值。反过来，通过在损失项中加入与系统相关的专门知识，或采用物理启发的特征归一化，也可以进一步提高预测精度。

## 3.相关实验介绍
```{figure} images/MeshGraphNets论文解读/四个实验.png
:width: 700px
:align: center

模型能够预测差异巨大的物理系统的动力学行为，从结构力学、布料到流体动力学。通过对以下系统进行仿真来展示这一点：
(a) 风中飘动的旗帜，(b) 发生变形的金属板，(c) 水绕过圆柱障碍物的流动，(d) 空气绕过飞机机翼截面的流动。
颜色映射在 (b) 中表示 von Mises 等效应力，在 (c)、(d) 中表示 速度场的 x 分量。
```

用仿真网格 Mt=(V, EM) 来描述系统在时刻 t 的状态，其中节点集合为 V，节点之间通过网格边 EM 连。每个节点 i 都关联一个参考的网格空间坐标 ui（用于张成/参数化仿真网格），以及希望建模的其他动力学量 qi。

对于欧拉（Eulerian）系统（图 2c、2d），在固定网格上建模连续场（例如速度场）的演化，此时 qi 表示在网格节点处对这些场的采样值。

对于拉格朗日（Lagrangian）系统，网格表示一个会运动、会变形的曲面或体（例如图 2a、2b）。此时除了固定的网格空间坐标ui之外，网格还包含一个额外的世界空间坐标 xi，用于描述网格在三维空间中的动态状态（图 3a）。

欧拉描述（Eulerian）：站在空间固定位置看，“场”随时间怎么变。网格固定，物理量在网格上流动。

拉格朗日描述（Lagrangian）：跟着物质点/网格点走，看“这个点”经历了什么。网格随物体运动变形，物理量绑在节点上随动。

## 4.模型具体介绍
### 4.1学习前向动力学（Learning Forward Dynamics）
任务是学习一个前向模型：在给定当前网格 Mt（以及可选的前 h 个历史网格）的情况下，预测网格在时刻 t+1 的动力学量。提出 MeshGraphNets：一种基于图神经网络的模型，采用Encode–Process–Decode（编码–处理–解码）架构，在其后接一个积分器（integrator）。图 1 给出了 MeshGraphNets 架构的可视化示意。

编码器（Encoder）：编码器将当前网格 Mt 编码为一个多重图（multigraph）G=(V, EM, EW)。网格节点对应图节点集合 V，网格边在图中变为双向的网格边 EM。这部分用于计算网格的内部动力学。对于拉格朗日系统，还会向图中加入世界空间边EW，以便学习诸如碰撞与接触等外部动力学——这类作用在网格空间中是非局部的。世界空间边依据空间邻近关系构造：给定一个固定半径 rW（数量级与最短网格边长度相当），如果节点 i 与 j 的世界坐标距离满足 |xi - xj| < rW，并且该节点对在网格中并非已相连，则在它们之间添加一条世界空间边。这样做鼓励模型通过世界空间边在三维空间中很近但在网格拓扑上很远的节点之间传递信息（见图 3a）

```{figure} images/MeshGraphNets论文解读/世界边.png
:width: 700px
:align: center

布料与球体发生交互的仿真示例。
(a) 用红色标出两个节点：它们在世界空间（world-space）中彼此很近，但在网格空间（mesh-space）中相距很远；在这种情况下，可能会在这两个节点之间创建一条世界空间边（world edge）。
(b) 在节点数相同的前提下，自适应重网格（adaptive remeshing）相比具有相同节点数的规则网格（regular mesh），能够实现显著更高的仿真精度。
```
```{figure} images/MeshGraphNets论文解读/处理器.png
:width: 950px
:align: center

```

```{figure} images/MeshGraphNets论文解读/解码器与状态更新器.png
:width: 950px
:align: center

```

```{figure} images/MeshGraphNets论文解读/输入输出.png
:width: 950px
:align: center

输入输出编码细节
```

### 4.2自适应重网格（Adaptive Remeshing）
自适应重网格算法通常由两部分组成：第一，识别仿真域中哪些区域需要较粗或较细的分辨率；第二，根据目标分辨率调整节点及其连接关系。只有第一部分需要关于具体物理系统类型的领域知识，这通常以启发式规则的形式出现。例如，在布料仿真中，一个常见的启发式是对高曲率区域进行加密，以确保弯曲动力学足够平滑（见图 3b）；而在计算流体力学中，常见做法是在壁面边界附近进行加密，因为那里往往会出现速度场的强梯度。

在本工作中，采用 尺寸场（sizing field） 方法 。尺寸场张量
```{figure} images/MeshGraphNets论文解读/公式1.png
:width: 100px
:align: center

```

通过编码网格中允许的最大“带方向的边长”，来指定期望的局部分辨率。对于一条边 uij，当且仅当满足
```{figure} images/MeshGraphNets论文解读/公式2.png
:width: 100px
:align: center

```

时，这条边被认为是“有效”的；否则它就太长，需要被拆分（split）。²在给定尺寸场之后，一个通用的局部重网格算法可以通过拆分所有无效边来细化网格，并在不产生新的无效边的前提下，尽可能合并（collapse）边以粗化网格。我们将这一重网格过程记为：
Mt = R(M, S)

学习式重网格（Learned remeshing）
为了在推理阶段也利用动态重网格在效率与精度上的优势，我们需要能够在测试时自适应地调整网格。然而，由于重网格依赖领域知识，如果在模型 rollout 的每一步都调用用于生成训练数据的那个特定重网格器，就会削弱学习模型带来的收益。为此，我们转而学习尺寸场模型（重网格中唯一依赖具体领域的部分）：使用与第 3.1 节相同的架构，并训练解码器输出 pi 来为每个节点生成一个尺寸张量。
在测试时，对每个时间步，我们同时预测下一步的仿真状态与尺寸场，然后使用一个通用的、与领域无关的重网格器 R，将自适应后的下一步网格计算为：
```{figure} images/MeshGraphNets论文解读/公式3.png
:width: 200px
:align: center

```

在三角网格上展示了这一流程；附录 A.3 描述了为此使用的简单通用重网格器。尺寸场本身与网格类型无关，但不同的网格类型可能需要不同的局部重网格器：四边形网格则可以简单地先拆分成三角网格来处理。

### 4.3模型训练（Model Training）
通过对解码器输出的逐节点（per-node）特征 pi 进行监督来训练动力学模型，具体做法是使用 pi 与其对应的真实值之间的 L2 损失。同样地，尺寸场（sizing field）模型也用真实尺寸场作为监督信号，并采用 L2 损失进行训练。
如果训练数据中没有尺寸信息——例如真实仿真器并未输出或提供该信息——我们仍然可以从仿真网格的样本中估计一个相容的尺寸场，并将该估计作为标签来训练。
```{figure} images/MeshGraphNets论文解读/弹性板实验.png
:width: 950px
:align: center

```
准静态仿真（quasi-static simulation）指的是：系统的演化过程足够慢，以至于惯性效应（质量×加速度）可以忽略，系统在每个加载增量结束时都近似处于静力平衡状态。

## 5.实验结果分析
```{figure} images/MeshGraphNets论文解读/泛化几何.png
:width: 700px
:align: center

(a) 我们模型在数据集 AIRFOIL 上的滚动预测结果（rollout）与真实结果的对比。自适应网格划分（adaptive meshing） 使我们能够在大尺度和小尺度上都准确地预测动力学行为。基于网格的 U-Net 基线模型在大尺度上可以做出较好的预测，但即使使用的网格单元数量是我们模型的四倍，它仍然无法解析更小尺度的结构。
(b) 在推理阶段，我们的模型可以扩展到远比训练时更大且更复杂的系统设置。
```

```{figure} images/MeshGraphNets论文解读/模型对比.png
:width: 700px
:align: center

(a) 模型优于基于 GCN 和 CNN 的基线方法。
(b) GNS 在布料数据集上会发散；加入 mesh-space 位置（GNS+mesh-pos）虽然有所帮助，但在动态网格上仍然失败。
(c) 使用学习得到或估计得到的尺寸场（sizing field）进行重网格，都能得到准确的 rollout。
(d) 足够多的消息传递步数对性能至关重要；同时限制历史长度可以通过避免过拟合来提升精度。
```

```{figure} images/MeshGraphNets论文解读/消耗时间对比.png
:width: 700px
:align: center

各种数据集的时间以及误差
```

## 6.综合创新点
1) Mesh-space + World-space 的双空间消息传递
2) 相对几何编码实现空间等变性
3) Encode–Process–Decode + 显式积分器
4) 学习“尺寸场 sizing field”实现动态重网格
5) 训练噪声 + 目标修正，让 one-step 训练支撑长 rollout